/*
 * Script cho trang dashboard chính (home.jsp).
 * Hỗ trợ bởi HomeServlet — xử lý tương tác UI phía client trên trang chủ.
 */

// Khởi tạo khi DOM sẵn sàng
document.addEventListener('DOMContentLoaded', function () {
    // Placeholder — logic trang chủ sẽ được bổ sung theo yêu cầu tính năng.
    console.log('Home page loaded.');
});
