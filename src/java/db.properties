# ============================================================
# Cấu hình kết nối cơ sở dữ liệu — đọc bởi utils.DBContext
# Chỉnh sửa các giá trị dưới đây cho phù hợp với môi trường cục bộ.
# KHÔNG commit file này nếu chứa mật khẩu thật lên version control.
# ============================================================

# Driver JDBC — dùng MySQL Connector/J
db.driver=com.mysql.cj.jdbc.Driver

# URL kết nối — thay "lib_manager" bằng tên database thực tế nếu khác
db.url=jdbc:mysql://localhost:3306/library_management_swp391?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true

# Tên đăng nhập MySQL
db.username=root

# Mật khẩu MySQL (để trống nếu không có mật khẩu)
db.password=123456
